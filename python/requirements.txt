numpy>=1.26
torch>=2.2
pyyaml>=6.0
tqdm>=4.66
# coremltools is optional; install with: pip install '.[export]'

# Sleep-AI LoRA fine-tune pipeline (Apple Silicon only)
mlx>=0.18; platform_system == "Darwin" and platform_machine == "arm64"
mlx-lm>=0.18; platform_system == "Darwin" and platform_machine == "arm64"
